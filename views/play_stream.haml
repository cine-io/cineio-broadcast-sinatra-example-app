%script{:src => '//cdn.cine.io/cineio.js'}
:javascript
  window.onload = function(e) {
    CineIO.init("#{@publicKey}");
    var streamId = "#{@stream.id}"
      , domId = 'player';

    CineIO.play(streamId, domId);
  }

%h1= "Viewing stream #{@stream.id}"

#player
